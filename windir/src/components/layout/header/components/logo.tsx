import classes from '../header.module.css';
import fonts from '@/styles/fonts.module.css';
function Logo() {
    return <div className={`${classes.logo} ${fonts.runes}`}>windir</div>
}
export default Logo;